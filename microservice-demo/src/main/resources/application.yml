server:
  port: 8081

spring:
  application:
    name: microservice-demo
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/studentdemo?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: vardhan
    hikari:
      initialization-fail-timeout: 0
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create