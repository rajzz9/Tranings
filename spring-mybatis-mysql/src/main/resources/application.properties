# Changing application context
# server.context-path=/erudio-restful-api

# Logging setting
logging.level.= INFO
logging.level.com.vardhan.mybatis.springmybatismysql = DEBUG

spring.mvc.converters.preferred-json-mapper=gson

# Server configuration
server.port=8080

#PackagesToScan
entitymanager.packagesToScan=com.vardhan.mybatis.springmybatismysql.models

# Database
dataSource.driverClassName=com.mysql.jdbc.Driver
dataSource.url=jdbc:mysql://localhost:3306/test_mybatis
dataSource.username=root
dataSource.password=vardhan
datasource.initialize=true

spring.datasource.initialize=true

# MyBatis
mybatis.typeAliasesPackage=com.vardhan.mybatis.springmybatismysql.models
mybatis.mapperLocations=classpath*:**/mappers/*.xml